## Summary

What does this PR change, and why?

## Checklist

- [ ] `make test` passes (or equivalent `cargo test` + `pytest`).
- [ ] `make check-oracle` passes if golden vectors or numeric paths changed.
- [ ] `make lean` passes if `lean/` changed.

## Notes

Optional: links to issues, Lean module names, or benchmark results.
